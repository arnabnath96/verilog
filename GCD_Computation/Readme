*** GCD Computation ***

Logic : 
int gcd(int a, int b) 
{ 
    if (a == b) 
        return a; 
    else if (a > b) 
        return gcd(a-b, b); 
    else
        return gcd(a, b-a); 
}

Video : YouTube > HARDWARE MODELLING USING VERILOG by IIT KHARAGPUR - Prof Indranil Sengupta
